<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mathbank.problem.mapper.ProblemMapper">

    <resultMap id="ProblemListResultMap" type="com.mathbank.problem.dto.ProblemListDto">
        <id     property="id"        column="id"/>
        <result property="title"     column="title"/>
        <result property="createdAt" column="created_at"/>
        <collection property="tagList" ofType="com.mathbank.problem.domain.Tag">
            <id     property="id"        column="tag_id"/>
            <result property="tagType"   column="tag_type"/>
            <result property="tagValue"  column="tag_value"/>
            <result property="sortOrder" column="sort_order"/>
        </collection>
    </resultMap>

    <resultMap id="ProblemDetailResultMap" type="com.mathbank.problem.dto.ProblemDetailDto">
        <id     property="id"          column="id"/>
        <result property="title"       column="title"/>
        <result property="content"     column="content"/>
        <result property="imagePath"   column="image_path"/>
        <result property="answer"      column="answer"/>
        <result property="explanation" column="explanation"/>
        <result property="createdBy"   column="created_by"/>
        <result property="createdAt"   column="created_at"/>
        <result property="updatedAt"   column="updated_at"/>
        <collection property="tagList" ofType="com.mathbank.problem.domain.Tag">
            <id     property="id"        column="tag_id"/>
            <result property="tagType"   column="tag_type"/>
            <result property="tagValue"  column="tag_value"/>
            <result property="sortOrder" column="sort_order"/>
        </collection>
    </resultMap>

    <!-- 다중 태그 + 키워드 검색 (조건 없으면 전체 조회와 동일) -->
    <select id="search" resultMap="ProblemListResultMap">
        SELECT p.id, p.title, p.created_at,
               t.id AS tag_id, t.tag_type, t.tag_value, t.sort_order
        FROM (
            SELECT id, title, created_at
            FROM problem
            <where>
                <if test="keyword != null and keyword != ''">
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </if>
                <if test="tagIds != null and tagIds.size() > 0">
                    AND id IN (
                        SELECT pt.problem_id
                        FROM problem_tag pt
                        WHERE pt.tag_id IN
                        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
                            #{tagId}
                        </foreach>
                        GROUP BY pt.problem_id
                        HAVING COUNT(DISTINCT pt.tag_id) = ${tagIds.size()}
                    )
                </if>
            </where>
            ORDER BY created_at DESC, id DESC
            LIMIT #{size} OFFSET #{offset}
        ) p
        LEFT JOIN problem_tag pt ON p.id = pt.problem_id
        LEFT JOIN tag t ON pt.tag_id = t.id
        ORDER BY p.created_at DESC, p.id DESC, t.sort_order ASC
    </select>

    <select id="countSearch" resultType="int">
        SELECT COUNT(*)
        FROM problem
        <where>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="tagIds != null and tagIds.size() > 0">
                AND id IN (
                    SELECT pt.problem_id
                    FROM problem_tag pt
                    WHERE pt.tag_id IN
                    <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
                        #{tagId}
                    </foreach>
                    GROUP BY pt.problem_id
                    HAVING COUNT(DISTINCT pt.tag_id) = ${tagIds.size()}
                )
            </if>
        </where>
    </select>

    <select id="findById" parameterType="Long" resultType="com.mathbank.problem.domain.Problem">
        SELECT id, title, content, image_path, answer, explanation, created_by, created_at, updated_at
        FROM problem
        WHERE id = #{id}
    </select>

    <select id="findDetailById" parameterType="Long" resultMap="ProblemDetailResultMap">
        SELECT p.id, p.title, p.content, p.image_path, p.answer, p.explanation,
               p.created_by, p.created_at, p.updated_at,
               t.id AS tag_id, t.tag_type, t.tag_value, t.sort_order
        FROM problem p
        LEFT JOIN problem_tag pt ON p.id = pt.problem_id
        LEFT JOIN tag t ON pt.tag_id = t.id
        WHERE p.id = #{id}
        ORDER BY t.sort_order ASC
    </select>

    <insert id="insert" parameterType="com.mathbank.problem.domain.Problem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO problem (title, content, image_path, answer, explanation, created_by)
        VALUES (#{title}, #{content}, #{imagePath}, #{answer}, #{explanation}, #{createdBy})
    </insert>

    <update id="update" parameterType="com.mathbank.problem.domain.Problem">
        UPDATE problem
        SET title       = #{title},
            content     = #{content},
            image_path  = #{imagePath},
            answer      = #{answer},
            explanation = #{explanation},
            updated_at  = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM problem WHERE id = #{id}
    </delete>

    <insert id="insertTags">
        INSERT INTO problem_tag (problem_id, tag_id) VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{problemId}, #{tagId})
        </foreach>
    </insert>

    <delete id="deleteTagsByProblemId">
        DELETE FROM problem_tag WHERE problem_id = #{problemId}
    </delete>

    <select id="findIdsByCondition" resultType="Long">
        SELECT DISTINCT p.id
        FROM problem p
        INNER JOIN problem_tag pt_grade ON p.id = pt_grade.problem_id AND pt_grade.tag_id = #{gradeTagId}
        <if test="semesterTagId != null">
            INNER JOIN problem_tag pt_sem ON p.id = pt_sem.problem_id AND pt_sem.tag_id = #{semesterTagId}
        </if>
        <if test="unitTagIds != null and unitTagIds.size() > 0">
            INNER JOIN (
                SELECT problem_id
                FROM problem_tag
                WHERE tag_id IN
                <foreach collection="unitTagIds" item="uid" open="(" separator="," close=")">
                    #{uid}
                </foreach>
                GROUP BY problem_id
                HAVING COUNT(DISTINCT tag_id) >= 1
            ) pt_unit ON p.id = pt_unit.problem_id
        </if>
        INNER JOIN problem_tag pt_diff ON p.id = pt_diff.problem_id AND pt_diff.tag_id = #{difficultyTagId}
    </select>

</mapper>
