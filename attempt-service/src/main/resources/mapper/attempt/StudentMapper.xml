<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mathbank.attempt.mapper.StudentMapper">

    <!-- grade는 문자열 컬럼으로 자체 보관하므로 problem-service의 tag 테이블 JOIN이 필요 없다. -->

    <insert id="insertStudent" parameterType="com.mathbank.attempt.domain.Student"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student (name, grade, memo)
        VALUES (#{name}, #{grade}, #{memo})
    </insert>

    <select id="findAll" resultType="com.mathbank.attempt.dto.StudentListDto">
        SELECT id, name, grade, memo, created_at
        FROM student
        ORDER BY created_at DESC, id DESC
    </select>

    <select id="findById" resultType="com.mathbank.attempt.domain.Student">
        SELECT id, name, grade, memo, created_at
        FROM student
        WHERE id = #{id}
    </select>

    <update id="updateStudent" parameterType="com.mathbank.attempt.domain.Student">
        UPDATE student
        SET name = #{name}, grade = #{grade}, memo = #{memo}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM student WHERE id = #{id}
    </delete>

</mapper>
